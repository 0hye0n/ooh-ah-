#include <bits/stdc++.h>
using namespace std;
typedef long long lld;
lld n, m;
int main() 
{
	cin.tie(NULL);
	ios_base::sync_with_stdio(false);
	cin>>n>>m;
	cout<<abs(n-m);
	return 0;
}